# DiveEdit build/runtime dependencies for Python 3.10 on Windows.
#
# Customer machines do not install this file. The release installer ships a
# PyInstaller onedir app with Python and these packages already bundled.
# Use this file only on the build/development machine:
#
#   pip install -r requirements.txt

faster-whisper>=1.2.0
ctranslate2>=4.6.0
av>=13.0
huggingface_hub>=0.24

oneocr>=1.0.12
opencv-python>=4.10
numpy>=1.26
pillow>=10

pyyaml>=6.0
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.8

pywebview>=6.0
pywin32>=306; platform_system == "Windows"

pyinstaller>=6.17
nvidia-cudnn-cu12>=9.0; platform_system == "Windows"
